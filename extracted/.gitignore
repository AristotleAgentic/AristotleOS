node_modules
dist
.env
# pnpm is the only supported package manager (workspace: protocol). Keep
# pnpm-lock.yaml as the single source of truth; never commit npm/yarn lockfiles.
package-lock.json
yarn.lock
coverage
*.log
secrets/
services/*/data/
backups/
.aristotle/
# Local control plane runtime artifacts (created by `pnpm local:up`).
.aristotle-local-stack.json
/data/
/logs/
.tmp/
reports/
# Supply-chain build outputs (generated in CI; attested, not committed).
.artifacts/
sbom.json
playwright-report/
test-results/
.playwright/

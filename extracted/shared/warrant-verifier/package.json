{
  "name": "@aristotle/warrant-verifier",
  "version": "0.1.0",
  "description": "Standalone public warrant verifier for AristotleOS. A third party who only holds the warrant + the canonical action hash + the trust anchors can validate a Warrant without access to the Commit Gate or any private state. Pure function + Node http handler.",
  "license": "Apache-2.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@aristotle/execution-control-runtime": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/index.test.ts"
  }
}

{
  "name": "@aristotle/nonce-store",
  "version": "0.1.0",
  "description": "Durable replay-protection nonce store for AristotleOS. Implements the NonceSeenSet interface from @aristotle/execution-control-runtime with in-memory + filesystem-backed (append-only JSONL) backends and TTL eviction. Closes ROADMAP_TO_100.md Category 1 'durable NonceSeenSet'.",
  "license": "Apache-2.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/index.test.ts"
  },
  "dependencies": {
    "@aristotle/execution-control-runtime": "workspace:*"
  }
}

{
  "name": "@aristotle/tenant-onboarding",
  "version": "0.1.0",
  "description": "Tenant onboarding primitive for AristotleOS. Bootstrap a new tenant with a Meta Authority Envelope, an initial Ward, an Authority Envelope, and a bootstrap agent subject in one signed, validator-checked call.",
  "license": "MPL-2.0",
  "private": false,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@aristotle/governance-core": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --import tsx --test src/index.test.ts"
  }
}

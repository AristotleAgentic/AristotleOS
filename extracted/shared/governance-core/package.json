{
  "name": "@aristotle/governance-core",
  "version": "0.1.0",
  "license": "MPL-2.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "Runtime governance chain for AristotleOS: Meta Authority Envelope -> Ward -> Authority Envelope -> Warrant -> Commit Gate -> Execution -> GEL Record. No action reaches execution unless the chain is complete and valid at the moment of commit.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "check": "tsc -p tsconfig.check.json",
    "test": "tsx src/test/run.test.ts",
    "start": "node dist/index.js"
  }
}

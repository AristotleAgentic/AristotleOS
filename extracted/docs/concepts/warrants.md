# Warrants

A Warrant is scoped, time-bound authorization for one execution.

It exists because autonomous systems should not retain standing machine power. The Commit Gate may allow an action only by issuing a warrant tied to action hash, policy hash, authority hash, Ward, expiry, and single-use semantics.

Developers validate the warrant before calling the real tool.

The warrant id and hashes are committed into GEL.
